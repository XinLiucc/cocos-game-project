# 项目计划（草稿，持续完善中）

## 基本信息
- **游戏类型**：开罗式经营模拟游戏
- **题材**：尚未最终确定，见下方"未来构想"。当前先做汽车修理厂练手项目，
  不追求题材本身成为正式产品（短剧公司、理财咨询公司等题材均已讨论并暂时搁置）
- **引擎**：练手阶段改用 Godot（Cocos Creator/TypeScript 暂缓，原因见下方"开发环境"一节）；
  正式产品若仍要主打微信/抖音小游戏，届时需要评估换回 Cocos / Egret / Laya
  （微信官方支持小游戏适配的引擎目前只有这三个，Godot 无官方小游戏导出方案）
- **首发平台**：微信小游戏 / 抖音小游戏（暂不锁定，见下方练手项目说明）
- **后续可能平台**：Steam、国际 App Store / Google Play、国内 Android 应用商店矩阵
- **资质/版号**：暂缓，先做技术验证，后续再推进

## 当前重点：练手项目 —— 汽车修理厂
目的：纯学习题材，不追求题材惊艳、也不追求成为正式产品，先跑通一遍完整游戏开发流程
（UI、数据驱动、回合结算、存档），积累经验后再回头做正式题材。
引擎改用 Godot（原因见"引擎"字段），不再纠结是否为微信/抖音小游戏铺路——如果以后
正式产品确定要上小游戏平台，届时再单独评估是否需要换回 Cocos/Egret/Laya 重新练一遍
引擎特定的部分。

核心循环（草案）：
1. 招人：社会招聘有经验的员工（贵，立即可用）或招学徒（便宜，需要培养/带教才能独立干活）
2. 接单：修理不同种类的车，可能需要特定工具/道具、特定技能才能高效完成
3. 结算：修车产出收入 + 口碑，口碑影响后续能接到的客户/车辆档次
4. 升级：用赚到的钱升级修理厂设施（工位、工具、店面），拓宽接单能力上限
5. 飞轮：口碑↑ → 更好的客户/订单 → 更多收入 → 招人/升级 → 口碑↑

待细化：
- [x] 回合/时间结构：真实时间自动流逝，`seconds_per_day`（占位 3 秒）驱动天数，30 天一个月
- [ ] 学徒培养机制的具体数值（培养时长、成长曲线，当前 v0 占位值有待重新平衡）
- [ ] 特殊道具/工具的种类和获取方式
- [ ] 角色技能系统（区别于现有五维属性，特定技能可产生减少维修时间等专项效果，
  2026-08-18 去掉带教固定耗时折扣时冒出的方向，具体设计待展开）
- [x] 美术风格：像素风（练手项目对个人开发者更友好，且开罗式游戏本身路子对）
- [ ] 变现方式（内购、广告、双平台差异）
- [x] 开发环境搭建：改用 Godot 后不再需要跨系统分工，编辑器已安装（见下方"开发环境"一节）

## 开发环境
- 2026-07-24 更新：练手项目引擎由 Cocos Creator 改为 Godot。原因：Cocos Creator 无官方 Linux
  编辑器，之前只能靠 Ubuntu 写代码 + Windows 跑编辑器的双系统分工解决，操作繁琐；另外调研发现
  Cocos 生态目前没有成熟的官方 MCP 编辑器集成，第三方插件（如 DaxianLee/cocos-mcp-server、
  FunplayAI/funplay-cocos-mcp 等）维护度存疑，练手阶段体验成本偏高。
- 既然练手项目不再绑定微信/抖音小游戏首发目标，之前"换引擎会丢失微信小游戏官方支持"的顾虑暂不适用。
- Godot 官方支持 Linux 编辑器，理论上不再需要双系统分工，两台机器都能直接装 Godot + 跑
  Claude Code，具体是否还要跨机器同步、用什么方式同步，等实际用起来再定。
- 2026-07-25 更新：Ubuntu 机器上完成实际安装。Godot 4.7.1 官方二进制装在 `~/Applications/Godot/`，
  软链到 `~/.local/bin/godot`；桌面应用图标也配好了（`~/.local/share/applications/godot.desktop`）。
  项目骨架建在仓库内的 `car_repair_shop/` 子目录（`project.godot` 手写最小配置，暂无主场景）。
  MCP 一开始选了 GDAI MCP（3ddelano/gdai-mcp-plugin-godot），但核实后发现两个问题：插件包不在
  GitHub 上直接可下载（要走 Buy Me a Coffee 页面），且实际是 $19 一次性付费（不是免费，早前调研
  的信息有误）。改用 **tugcantopaloglu/godot-mcp**：MIT 协议真开源，157 个工具，明确测试过
  Godot 4.7，`git clone` 到 `~/Applications/godot-mcp/` 后 `npm install && npm run build` 装好，
  已通过 `claude mcp add` 接入 Claude Code（显示 Connected）。运行时调试用的
  `mcp_interaction_server.gd` 已拷到 `car_repair_shop/scripts/` 并注册为 autoload
  `McpInteractionServer`，还没在编辑器里实测过。
- 历史记录（Cocos Creator 阶段的分工方案，供参考）：曾用 Ubuntu 写代码（Claude Code + git）+
  Windows 跑 Cocos Creator 编辑器，通过 git 仓库同步，因为 Cocos Creator 无官方 Linux 版；
  曾考虑换引擎规避这个问题，但当时微信官方文档中完成小游戏适配的引擎只有 Cocos、Egret、Laya，
  Unity 无官方支持，换引擎会丢失当初选 Cocos 的核心原因，因此暂缓换引擎、改用双系统分工。

## 未来构想（先记录，暂不推进）
聊天中冒出的一个更大的长线构想，比"短剧公司"和"理财咨询公司"都更有潜力，
但规模远超练手项目，先记下来，等汽车修理厂练手完成、引擎熟悉之后再评估是否作为正式题材：

**技校 → 集团** 的成长路线：
- 起步开一家技校，社会招生，培养学生的各类技能（比如烹饪、维修、理财等，具体技能树待定）
- 靠学费/培训费维持初期运营
- 当某项教学特别突出时，可以开设对应的子公司（比如烹饪强 → 开餐饮公司）
- 把培养出的优秀学生分配到子公司工作，子公司创造新收入
- 逐步在不同行业开子公司，最终滚成一个多元化集团
- 优点：天然的"阶段性解锁全新玩法/行业"作为里程碑，比数值缓慢增长更有成长感
- 需要先想清楚：技能树设计、子公司数量与行业选择、子公司之间是否有联动玩法

## 里程碑（粗略，待细化）
1. 环境搭建 + Cocos Creator 上手 demo
2. 核心循环最小可玩版本（无美术，跑通数值）
3. 美术资源接入 + UI 打磨
4. 小游戏平台接入测试（微信/抖音 SDK）
5. 内测 / 资质与上架准备

## 更新记录
- 2026-07-21：创建计划文档，确定引擎（Cocos Creator）与首发平台（微信/抖音小游戏），
  确定题材方向为"短剧制作公司"。
- 2026-07-22：讨论多个题材（短剧公司、理财咨询公司），发现题材本身不好把握（不了解行业 /
  数值进步感不明显）。决定先不纠结正式题材，做"汽车修理厂"作为练手项目跑通引擎和核心循环。
  聊天中额外冒出"技校 → 按特长孵化子公司 → 集团"的长线构想，记录为未来方向，暂不推进。
- 2026-07-23：确定汽车修理厂练手项目的美术风格为像素风；再次明确该项目定位纯学习题材，
  不追求成为正式产品。讨论开发环境搭建，发现 Cocos Creator 无官方 Linux 编辑器，一度考虑换引擎，
  最终确定不换引擎，改为双系统分工（Ubuntu 写代码 + Windows 跑编辑器，git 同步）。新增 CLAUDE.md
  记录跨设备通用约定（提交语言、文档语言、设计决策节奏等），避免仅存于本机 memory 的信息在切换
  设备后丢失。
- 2026-07-24：Windows 开发环境装好后，讨论如何用 MCP 服务器连接编辑器（类似 Godot 的玩法），
  调研发现 Cocos Creator 没有成熟的官方 MCP 集成，只有若干第三方插件、维护度存疑。借此机会重新
  评估引擎选择：由于汽车修理厂只是练手项目，不必绑死微信/抖音小游戏首发目标，决定改用 Godot
  （用户已有一定 Godot 经验，官方支持 Linux，MCP 生态更成熟），不再需要 Ubuntu/Windows 双系统
  分工。是否要为正式产品上线微信/抖音小游戏而换回 Cocos/Egret/Laya、重新练一遍引擎特定部分，
  留到练手项目完成、需要定型正式产品时再评估。
- 2026-07-25：Ubuntu 机器上实际装好 Godot 4.7.1，建了项目骨架 `car_repair_shop/`。MCP 集成方案从
  GDAI MCP（发现是付费）改为免费开源的 tugcantopaloglu/godot-mcp，已接入 Claude Code。细节见上方
  "开发环境"一节。
- 2026-08-01：首次实测 MCP 全流程——通过 Claude Code 用 MCP 建场景、加节点、跑项目、截图，确认
  `McpInteractionServer` autoload 正常工作，里程碑 1（环境搭建 + demo）算是真正落地。在此基础上
  搭了核心循环里"接单→结算"这一段的最小可玩版本（v0）：新增 `GameState.gd`（autoload，管理金钱/
  口碑全局状态，通过 signal 通知 UI 刷新）、`Main.tscn` + `Main.gd`（点击按钮触发 3 秒倒计时修车，
  完成后加钱加口碑）。招人、升级、飞轮部分还没做；数值全是占位值，之后再细化。过程中发现一个
  MCP 工具的坑：`attach_script` 是在隔离的无头进程里编译脚本，不会加载项目的 autoload 列表，所以
  脚本里不能直接写 `GameState.xxx` 这种编译期全局标识符，得改成运行时 `get_node("/root/GameState")`
  查找，否则会报"找不到标识符"。
- 2026-08-02：给"接单"加了车型区分。新增 `OrderType.gd`（`Resource` 数据模板：车型名/耗时/报酬/
  口碑加成）和 3 条具体数据（`resources/orders/` 下的 `sedan.tres`/`suv.tres`/`sports_car.tres`，
  耗时报酬递增）。`Main.gd` 接单时从这 3 种里随机抽一单，用它的数值驱动倒计时和结算，UI 会显示
  当前车型。实测三种车型都能正确随机出现、数值对得上。目前还是纯随机接单，没有工具/技能门槛、
  也没有按口碑解锁车型档次，这些留到招人机制定下来后再接上。
  同一晚上又加了"招人 v0"占位版：`GameState` 新增 `worker_count`，花钱雇工人（费用随人数递增：
  100/150/200...），每个工人让修车耗时打九折（`repair_time_multiplier`）。这只是让"招人"这个
  动作先有反馈，明确不代表最终数值——PLAN.md 里"社会招聘 vs 学徒培养"两条线的具体机制和数值还没
  定，之后要重新设计时这版占位逻辑大概率会整个换掉。实测雇人后接单确实变快、费用递增也生效。
- 2026-08-03：给车型加了口碑解锁门槛（占位数值，未细化）。`OrderType.gd` 新增 `min_reputation`
  字段，家用轿车 0（一直可接）、SUV 10、跑车 25；`Main.gd` 接单时先过滤出口碑达标的车型再随机抽。
  用 godot-mcp 实测：口碑 0 时只出家用轿车，涨到 10 后 SUV 开始出现、跑车仍不出现，涨到 25 后跑车
  也正常出现，三档过滤都验证通过。门槛数值纯占位，之后跟招人/学徒机制一起细化时可能整体重调。
  同一晚上讨论了接下来先补齐流程骨架（设施升级、学徒）还是先抠数值，决定先把骨架搭完，数值统一
  放到骨架完整、能看清系统间制约关系时再细化——学徒培养的具体数值本来就是要慢慢想的设计题，
  现在硬啃容易白费。据此加了"设施升级 v0"占位版：`GameState` 新增 `facility_level`，花钱升级
  工位（费用递增：150/250/350...），每级让每单收入提升 15%（`payout_multiplier`），跟招人是
  两条独立杠杆（招人管修车快慢，升级管每单赚多少）。`Main.gd`/`Main.tscn` 加了对应 UI。用
  godot-mcp 实测：升级两级后 payout_multiplier 变 1.3，实际收入按比例四舍五入正确，真实点击
  升级按钮也验证了扣费和禁用态。这版数值同样是纯占位，之后可能整体重调。
  下一步计划：核心循环骨架只剩"学徒培养"这块空白（社会招聘 vs 学徒的具体机制、培养时长、成长
  曲线都还没定），下次从这里继续。
- 2026-08-04：确定学徒机制的核心框架并搭好骨架版。设计思路：学徒需要熟练工带教（雇学徒要求
  `worker_count >= 1`）；学徒本身能接活，报酬低但主要给经验；经验攒够后可以"考试"，通过后
  学徒等级 +1（这版必过，失败率留白）；学徒要发月薪，没有招募数量上限，靠每天订单量自然限制
  规模，不需要额外设计人为上限。这个设计顺带牵出了一直悬着的"回合/时间结构"问题——月薪要按
  周期结算，得先有"月"这个时间节点，因此上一版先把"真实时间自动流逝"的时间系统搭了起来。
  学徒等级这版简化成一个共享池子属性，不区分个体学徒身份。新增
  `apprentice_count`/`apprentice_level`/`apprentice_xp`，`Main.tscn` 加了日期显示、学徒信息、
  招学徒/学徒实习/考试三个按钮。用 godot-mcp 实测：没有熟练工时招学徒按钮确实被挡住；招到
  学徒后点"学徒实习"能正确加钱加经验；经验攒够后"考试"按钮解锁，点击后等级 +1、经验按余数
  结转、月薪按新等级提高；直接调用月末结算函数验证了工资扣款金额精确无误。全部数值（招募费、
  月薪、实习报酬、考试经验门槛）都是占位值，之后要重新平衡；考试的失败率、学徒是否区分个体、
  带教是否真正占用熟练工产能这些细节也还没定，属于故意简化留白的部分。至此核心循环骨架
  （接单、招人、学徒、设施升级、时间流逝）算是完整跑通了。
  当晚实机试玩后用户反馈：熟练工（`worker_count`）没有工资，只有学徒扣月薪，两者不对称——
  这跟最初"社会招聘/学徒都要发工资，靠订单量自然限制规模"的构想不一致，得补上熟练工的
  工资。用户还指出"很多数值都没有联动起来"，没有展开具体所指，需要下次一起过一遍现有系统
  找出哪些地方是平行、没有真正互相影响的——已知的候选：①"带教"目前只是个数量门槛
  （`worker_count >= 1`），并不会真的占用熟练工产能；②学徒实习是脱离 `OrderType`/车型体系
  的固定值，不吃口碑门槛也不区分车型；③学徒练到再高等级好像也不会反过来影响主循环接单
  （除了自己那点低报酬实习收入）；④设施升级的 `payout_multiplier`、招人的
  `repair_time_multiplier`、学徒体系三者是三条独立杠杆，彼此不联动。
  下一步计划：先补熟练工工资，再跟用户过一遍上面这些"没联动"的点，确定哪些要打通、哪些
  维持独立设计是刻意为之，不要在没弄清楚具体所指前就动手瞎猜着改。
- 2026-08-05：过了一遍上次列的"没联动"疑点，达成共识：①熟练工工资、②学徒实习接车型体系
  今晚就做；③带教占产能、④学徒等级反哺主循环、⑤三条杠杆互相独立，先记录不动。过程中用户
  追问"是不是应该能同时接多辆车"，看代码确认现状确实是单任务锁（`repairing` 布尔），
  `worker_count` 只影响单辆车的修车速度，不是并发工位数——这正好是"带教占产能"缺的那块
  拼图（若 `worker_count` 变成"同时开几个工位"，带教就能自然表达成"每个熟练工带一个学徒、
  占一个工位"）。这是个结构性改动（单任务锁→多工位并发），牵动 UI 和结算逻辑，讨论后决定
  先记为下次专门讨论的设计题，今晚不展开。
  当晚完成的两件事：①`GameState.gd` 新增 `WORKER_SALARY_PER_HEAD`（占位 20/人），月末结算
  从"只扣学徒工资"改成"工人+学徒工资一起扣"，工人和学徒终于对称了。②学徒实习不再用固定的
  占位报酬/经验，改成从口碑已解锁的 `OrderType` 车型池里随机抽单，报酬按车型原价打 3 折
  （`APPRENTICE_PAYOUT_RATE`），经验按车型 `reputation_gain` 换算（`APPRENTICE_XP_PER_REPUTATION`
  = 15/点），顺带把接单/实习共用的"口碑过滤解锁车型"逻辑抽成了 `_get_available_orders()`
  共享方法。用 godot-mcp 实测：工人+学徒各一名时月末结算精确扣除 20+15=35；学徒实习抽到
  家用轿车时收入 +9（30×0.3）经验 +15（1×15），抽到 SUV 时收入 +18（60×0.3）经验 +30
  （2×15），口碑达标后车型池确实能抽到全部三种车。过程中踩了个 MCP 坑：`game_eval` 里
  用错了场景根节点路径（Main.tscn 的根节点其实叫 `root` 不是 `Main`），触发脚本错误后引擎
  卡在调试器断点上，后续所有 MCP 调用全部超时，得靠 `stop_project` 重启才能恢复——以后
  eval 前先确认节点路径。
  下一步计划：①带教是否真占产能（跟"能否同时接多辆车/工位并发"绑定的结构性设计题）、
  ②学徒等级反哺主循环（毕业/转正的意义）、③三条杠杆是否要打通，这三个留到下次专门讨论。
- 2026-08-06：逐个讨论上次留的三个设计题，先攻①和②，一次谈完（两者本来就互相牵连）。
  确定方向：
  - **工位** 由设施升级决定数量，是"同时能干活的人数"上限，跟 `worker_count`（员工总数）
    解耦——员工数可以比工位多（备用/轮班），但同时干活人数被工位卡住，逼着招人和升级设施
    两条线互相制约。玩家操作上不是摆弄抽象格子，而是直接把订单分配给具体空闲员工，只是
    活跃工作数不能超工位上限。
  - **带教＝熟练工+学徒配对占用同一个工位**：带教时学徒能处理熟练工能碰的车型（熟练工兜底）。
  - **学徒独立接单**受自身等级软性限制（参考开罗系游戏"软性影响"而非硬性门槛的惯常做法）：
    超出等级的车型不是不能接，而是效率/口碑加成打折，具体数值留白。学徒等级越高，独立接单
    表现越接近熟练工——这就是②"学徒等级反哺主循环"的答案。
  - ③（三条杠杆独立）借这版设计部分打通：工位把"招人"和"设施升级"两条线联系起来了；
    `payout_multiplier`（设施）目前看还是相对独立的一条，先不强行拼凑，维持现状。
  今晚开始落地①这版设计（结构性改动：单任务锁 → 多工位并发），分层推进：`GameState.gd`
  数据结构 → `Main.gd` 并发任务逻辑 → UI。订单生成仍用现有随机池逻辑，队列/流失机制
  （订单超时未接会扣声誉并流失客户，已达成共识但本轮不实现）留到下次。

  当晚把①落地到可玩状态：`GameState.gd` 新增 `station_count()`（`BASE_STATION_COUNT` +
  `STATION_PER_FACILITY_LEVEL * facility_level`，占位 1 + 1/级），设施升级现在同时决定工位数。
  连带去掉了 `repair_time_multiplier()`/`WORKER_SPEED_BONUS`——这是并发模型的必然推论：
  原本"人越多修一辆车越快"的假设，在"一个人独立干一单"的并发模型下不再成立，`worker_count`
  的意义改为"能同时填多少个工位"，不再兼职当速度加成（改动前跟用户确认过）。
  `Main.gd` 把 `repairing` 单例布尔改成 `active_jobs: Array[Dictionary]`（每个工位一个
  `{order, timer}`），每次点"接单修车"在 `active_jobs.size() < station_count() 且 <
  worker_count` 时动态建一个 `Timer` 子节点起一单，互不干扰地并发倒计时、各自结算。由于
  员工目前还是无个体身份的纯数量（`worker_count` 只是个数字），"选具体员工分配"简化成了
  "有空闲工位+空闲工人就能再开一单"，没有做真正的员工列表/点选 UI——等以后员工有个体属性
  （比如学徒等级挂到具体人身上）时再考虑要不要做具体点选。UI 复用了现有 Label（工位状态、
  剩余时间、最近完成记录合并显示在 `OrderLabel` 里，每帧刷新倒计时），没有新增场景节点，
  删掉了不再用的 `RepairTimer` 节点。用 godot-mcp 实测：`station_count()` 数值正确
  （facility_level=1 → 2 个工位）；连续接单能顶到工位上限后自动拒绝第三单
  （`worker_count=3, station_count=2` 时最多 2 单并发）；两单能各自独立倒计时、互不影响；
  真实点击"接单修车"按钮也能正确起单。过程中又踩了一次已知的 `game_eval` 坑——这次是
  单行 `for x: body(); return ...` 写法里 `return` 被 GDScript 解析成 for 循环体的一部分，
  导致过早返回/返回 null，不是引擎的锅，是我调试脚本自己的坑，用 `.map()` 换掉单行
  for 循环后正常；另外多行 eval 代码缩进用 tab/space 混用会导致调试器断点卡死，得
  `stop_project` 重启，这条老坑也又踩了一次。
  下一步：员工/学徒的个体化（给学徒挂等级、决定独立接单能力）、订单队列与超时流失机制，
  留到下次继续。

  实测完让用户在编辑器里自己点了一下，发现一个真 bug：新档 `worker_count=0` 时
  `active_jobs.size() < worker_count`（`0 < 0`）恒为假，"接单修车"永远点不动，新档彻底
  卡死——因为旧版本"接单修车"不看 `worker_count`，玩家自己就能亲自修第一单赚启动资金，
  这次改并发时漏掉了这条路。第一反应是加"店老板自己算隐式免费工位"的特例（`worker_count
  + 1`），用户反问"给点启动资金不就行了"，明显更简单，遂改用这个方案：`GameState.gd`
  新增 `STARTING_MONEY = 100`，正好等于雇第一个工人的费用，新档第一步只能是雇人，
  雇完就能正常接单。实测确认新档流程通畅。
- 2026-08-07：开始做"订单队列+超时流失机制"（上次骨架清单的第二项）。核心改动：接单从
  "点按钮即时随机生成一单"改成"订单自己按固定间隔冒出来排队，玩家在过期前点按钮处理队首，
  超时未处理则流失并扣口碑"，跟用户讨论确定用定时自动生成（而非"有空闲工位就自动补单"），
  数值先用占位值跑（队列容量 3、生成间隔 10s、单个订单超时 15s、流失扣口碑 1 点，之后再
  平衡）。`Main.gd` 新增 `pending_orders` 数组（结构同 `active_jobs`：`{order, timer}`）和
  `order_spawn_timer`；`_on_order_spawn_timer_timeout()` 按间隔尝试补单（队列未满才补，从
  `_get_available_orders()` 随机抽），`_on_pending_order_expired()` 处理超时流失（扣口碑、
  记录动态提示）；`_on_repair_button_pressed()` 改成消费队首订单而不是现场随机生成。
  `GameState.add_reputation()` 顺带加了 `max(0, ...)` 下限，避免流失惩罚把口碑打成负数。
  `_update_order_label()` 把队列和工位状态分两段展示。
  过程中发现 `OrderLabel` 内容变长后被下方固定布局的 `RepairButton` 挡住文字——原来的
  `Main.tscn` 是纯手写绝对坐标、没有自适应容器，队列多出的几行直接和按钮重叠。用
  godot-mcp 截图发现后，把 `RepairButton` 及以下所有节点的 `offset_top`/`offset_bottom`
  统一下移，并把窗口视口高度从默认 648 调到 800 腾出空间；这只是按当前数值量身适配的临时
  修复，`station_count` 会随设施等级继续增长，长远看固定绝对坐标布局迟早还会被撑爆，
  之后做真正的 UI 需要换成自适应容器（如 VBoxContainer）而不是继续手调坐标。
  用 godot-mcp 实测：调高时间倍率后验证了订单会自动生成入队、排到 3 单后不再新增、
  超时后从队列消失且口碑正确 -1（口碑本来是 0，验证了 `max(0,...)` 下限生效不会变负数）、
  点"接单修车"能正确取走队首订单转入工位倒计时。中途一度以为真实点击按钮没反应（active
  一直是 0），排查后发现是 8 倍速下修车耗时被压缩到零点几秒，检查请求的网络往返比修车还慢，
  订单在我确认前就已完工——换回 1 倍速后重新点击，看到「完成：家用轿车，收入 30，口碑 +1」，
  确认点击本身没问题，是测试时的观察时机误判。
  下一步：员工/学徒个体化（骨架清单最后一项）留到下次继续；`OrderLabel` 的绝对定位布局
  已经比较脆弱，下次做 UI 相关改动时可以考虑一并换成自适应容器。
- 2026-08-08：完成骨架清单最后一项——员工/学徒个体化。原本 `worker_count`/`apprentice_count`
  都是纯数字，学徒等级/经验还是全体共享的池子；改成 `GameState.employees` 数组，每个工人/学徒
  都是独立个体（`id`/`kind`/`busy`/`level`/`xp`）。连带把"实习同时只能一个人"（全局唯一
  `practicing` 布尔）改成真正并发——每个学徒都能独立开局实习、各自计时，这是个体化的自然推论，
  否则多个学徒还是在互相排队。接单流程也改成指定具体空闲工人
  （`game_state.idle_workers()[0]`），完工/结束实习后精确释放该员工的 `busy` 状态，不再是抽象的
  数量比较。月末工资结算按每个员工自己的 `kind`/`level` 求和，不再是"总人数×固定值"。
  讨论范围时一度以为这轮只做后端数据结构（用户最初选"先只做后端"），但被用户点破——"我们设计的
  每一个工人和学徒都是一个个体，而不是一个数字"，这才是个体化真正要解决的立场问题，于是改为直接
  做完整列表 UI：工人只读展示忙闲状态（工人目前没有差异化属性，只有身份和忙闲两个维度）；学徒
  每行独立显示等级/经验/月薪/状态，配"实习""考试"两个按钮直接对指定学徒操作——考试尤其被用户
  点名要"选人"而不是自动挑，因为选谁考试是玩家的节奏决策，不该被自动化代劳。
  连带处理了上次记录的技术债：`OrderLabel`（订单队列/工位倒计时）是全场景唯一每帧刷新的内容，
  之前跟其他按钮混排在同一绝对布局里，文字变长变短会把下面所有按钮顶飘，导致点击坐标不稳定
  （用户反馈"点击的位置也不确定"后定位到这个根因）。这次把整个界面从手写绝对坐标换成
  `VBoxContainer`/`HBoxContainer` 自适应布局，并把 `OrderLabel` 挪到整个界面最后一个节点——
  这样它每帧变化都不会影响任何按钮位置。
  用 godot-mcp 实测：真实点击雇工人/接单/实习/考试全部走通；两名学徒同时点"实习"确认真正并发
  （各自独立 `busy` 状态、独立计时、独立加经验，互不阻塞）；月末结算精确扣款（工人 20 + Lv1
  学徒 20 = 40，与预期完全一致）；考试通过后等级 +1、经验按余数结转、月薪按新等级重算，数值
  全部验证无误。
  带教占工位（熟练工+学徒配对）本轮特意没做，上次已经商量好留到下次，跟"工位分配到具体人"的
  点选逻辑一起处理会更自然（现在工位分配还是自动挑"第一个空闲工人"，不是玩家选人）。
  下一步：骨架清单彻底清空。可选方向——①带教配对（工位在具体员工间怎么分配，自动还是也做成
  点选）、②开始往数值平衡方向走（现在几乎所有数值都是占位值）、③继续打磨 UI（工人也显示一些
  差异化信息，或者给列表加排序/分组）。留到下次跟用户对齐先做哪个。
- 2026-08-09：选了②的一个具体切口——给员工加属性数值（呼应"工人目前没有差异化属性"这个老
  遗留项），讨论过程中方案不断长大，最终定了一套完整框架：
  - **五维属性**：机械/电气/钣喷（硬技能，决定能不能干、干多快，将来跟车型加权耗时联动）+
    细心/沟通（软技能，决定质量/口碑，沟通将来给"前台"等新工种留了钩子）。
  - **成长曲线**：属性总和有软上限，越接近上限单次训练涨幅越小——不是零和分配（练一项不用扣
    另一项），只是总量越高整体越难涨，这让新学徒容易速成一两个专精项，后期精益求精。
  - **两条独立成长路径**：①学徒自己报名培训；②带教——师傅修车时学徒在旁辅助，本单效率提升，
    同时给学徒攒"属性点"，玩家自己决定点数分给哪一项（不是自动加成），呼应"选人考试"那种
    "重要节点该是玩家决策"的一贯做法。
  - **考试通过率**：分级递增门槛，一级要求五维属性总和 > 100；不达标时每差 1 点通过率 -8%，
    达标则必过。门槛/扣分率数值先占位，之后再平衡。
  - 新工种（前台等）、成长曲线/带教点数/考试概率的具体实现，都还没做，只是定了框架方向。
  当晚落地了框架里最基础的一层：`GameState.gd` 新增 `ATTRIBUTE_KEYS`（5 项属性 key）和随机
  基础值范围（占位 5~20），招工人/招学徒时通过 `_random_base_attributes()` 各自独立生成随机
  属性存进 `employees` 字典的新字段 `attributes`；`Main.gd` 加了 `_format_attributes()`，
  工人/学徒列表行都追加显示属性数值。用 godot-mcp 实测：连续雇佣多名工人/学徒，每人属性值
  各自独立随机、互不相同，UI 上正确展示。
  下一步：训练/带教的具体成长逻辑、考试概率公式、车型按属性加权耗时，这三块是属性系统真正
  发挥作用的地方，留到下次继续；前台等新工种的具体设计也还没展开。
- 2026-08-10：落地属性成长三件事里的第一件——学徒自学训练。开工前先理顺了一个数值冲突：
  原来工人/学徒共用同一套随机基础值（5~20），运气好的新学徒一入职属性总和就能顶到 100，
  正好撞上 08-09 定的一级考试门槛，训练还没开始就已经练不动。改成工人/学徒分两套起点——
  工人是"已出师的熟练工"，随机 15~20~25（均值 20，贴近考试线）；学徒是白纸，五项统一从
  最低值 5 起步，全靠训练/带教走方向分化，不再随机。
  训练机制仿照 `OrderType` 的资源模式，新增 `TrainingCourse.gd` + `resources/courses/` 下
  5 个课程资源（机械/电气/钣喷/细心/沟通各一门，字段 course_name/attribute_key/cost/
  duration/base_growth），学徒选一门课"精准培养"对应的单项属性，而不是笼统"训练"。涨幅
  公式带软上限衰减：`growth = base_growth × max(0.1, 1 - 五维总和/150)`，越接近软上限
  （150，即均值 30/项）涨得越慢，但留了 0.1 的下限系数不会完全练不动。花费/时长/基础涨幅
  （cost 20、duration 8s、base_growth 8）都是占位数值，之后再平衡。
  `GameState.gd` 加了 `can_start_training`/`start_training`/`complete_training`，学徒训练期间
  `busy=true`（复用实习那套占用机制，训练和实习互斥）；`Main.gd` 给每个学徒行加了 5 个课程
  按钮，忙碌时全部禁用，完成后自动结算涨幅并解除占用。
  用 godot-mcp 实测：雇工人/学徒后属性分布符合预期（工人随机分布在 15~25，学徒五项均为 5）；
  真实点击"训练：电气系统课"——扣费 20、状态显示"训练中"、其余操作按钮全部禁用；等待课程
  时长后自动完成，电气 5→11（与公式手算的涨幅一致），状态恢复空闲、按钮重新可用，没有
  重复扣费或重复涨属性。中途 `game_eval` 工具本身出现过一次同一脚本被服务端重复执行导致
  数值翻倍的现象，排查后确认是 MCP 桥接层的重试问题，不是游戏逻辑的 bug——真实点击复测后
  数值完全正常，之后这类数值核对更依赖真实点击而不是单纯看 eval 返回值。
  下一步：属性成长的另外两件事——带教配对（依赖"工位分配到具体人"的点选逻辑，上次已经
  提过要跟这个一起做）、考试通过率公式——留到下次继续；车型按属性加权耗时也还没动。
- 2026-08-11：落地考试通过率公式（上次留的三块下一步之一）。原来的考试机制是"攒够经验值门槛
  才能考、一考必过"，改成"随时可以开考，通过率由五维属性总和决定"——`GameState.gd` 新增
  `exam_attribute_threshold(level)`（一级门槛 100，每级 +40，占位待平衡）和 `exam_pass_rate(id)`
  （门槛与属性总和的差值每点扣 8%，钳制在 `[1%, 100%]`，呼应用户要求的"最低 1% 最高 100%，
  留运气成分"）。跟用户确认了一个关键缺口：去掉经验门槛后拿什么防止玩家无脑连点直到过为止——
  用户选了"考试本身要花钱，无论成败都不退"，新增 `EXAM_COST`（占位 20）。`take_exam()` 返回
  值从 `bool` 改成 `Dictionary`（空字典=没能开考，否则 `{passed, rate}`），方便 UI 展示"通过
  概率+结果"这种比单纯真假更有信息量的反馈；`Main.gd` 按结果分别拼"通过（概率X%），晋升至
  LvN"或"未通过（概率X%），报名费打水漂"两种动态提示，考试按钮文字也直接显示当前报名费和
  实时通过率。原有的经验值/`apprentice_xp_required_for_level` 机制本身没动（练习攒经验依然
  有效），但考试不再消费它，目前处于"没有消费者"的悬空状态，先留着，之后要不要重新利用或去掉
  待讨论。
  用 godot-mcp 实测：`game_eval` 验证了边界行为——属性总和刚好等于门槛时 rate=100（必过，
  等同旧版"达标必过"）、五维全 5（新学徒起点）时 rate 被负数钳制到 1%；连续 5 次调用
  `take_exam` 验证了每次都正确扣款 20、失败不掉级不涨级、通过后等级+1 且下一级门槛立刻变高
  （140），通过率应声跌回 1%——"分级递增门槛"生效。真实点击测试：先在 1% 通过率下点击考试
  按钮，确认扣款且大概率失败（等级不变）；再把属性调到远超门槛（sum=200, rate=100%）后
  真实点击，确认必过、等级 1→2、`last_result_text` 正确显示"通过（概率100%），晋升至 Lv2"；
  又验证了金钱不足 `EXAM_COST` 时 `can_take_exam` 正确返回 false（按钮应禁用）。
  下一步：带教配对、车型按属性加权耗时——还是上次留的另外两块，继续留到下次；经验值机制何去
  何从（重新利用还是移除）也需要找时间跟用户对齐。
- 2026-08-12：落地带教配对（属性成长第二条路径，上次留的两块之一）。设计上用户提议：带教产出
  的属性点固定是整数，数量由师傅的"细心"属性决定（细心越高教得越好），学徒具体加到哪项属性
  仍由玩家手动分配，不自动指定——这样"细心"这个软技能第一次有了具体的数值杠杆（之前只挂了
  "质量/口碑"这种模糊用途）。空闲工人+空闲学徒配对，一起占一个工位处理真实订单（复用现有的
  工位并发/队列机制，不是像训练那样的独立虚拟动作），耗时按 `MENTOR_TIME_REDUCTION_RATIO`
  （占位 0.2，即快 20%）打折；`GameState.gd` 新增 `mentor_points_earned(master_precision)`
  （`MENTOR_POINTS_BASE + precision / MENTOR_PRECISION_DIVISOR`，占位 1 和 10）、
  `can_start_mentoring`、`add_attribute_points`、`allocate_attribute_point`；学徒新增
  `pending_points` 字段（待分配属性点，整数）。`Main.gd` 复用 `_on_job_complete` 处理带教完工
  （通过 job 字典里的 `apprentice_id` 字段区分是否带教单，而不是另开一套完工逻辑），完工时给
  学徒记入待分配点数并解除双方 busy；UI 给每个空闲学徒行加了"选师傅下拉+带教修车"按钮和
  （有待分配点数时才出现的）5 个"+属性"分配按钮。
  实测过程中先用真实点击验证，发现"带教修车"和"+属性"按钮点了没反应——排查后发现是真 bug：
  学徒这一行原本就有实习/考试/5 门训练课程按钮，这次又加了配对下拉+带教按钮+最多 5 个分配
  按钮，一整行塞进同一个 `HBoxContainer` 后总宽度远超窗口默认宽度（1152px），右侧按钮虽然
  在场景树里、`game_get_ui` 也能读到坐标，但已经渲染到窗口外，点击自然没反应——不是这次改动
  引入的新东西，是既有"一行塞所有按钮"的模式终于被撑爆了（此前 08-07/08-08 也踩过纵向布局
  被撑爆的坑）。改成学徒每行用 `VBoxContainer` 包一个多行结构（状态/训练课程/带教配对/属性
  分配各占一行 `HBoxContainer`），比继续加宽窗口更能扛住以后还会增加的按钮。改完用 godot-mcp
  真实点击复测：带教修车按钮能正确起单、扣双方 busy、工位计数 +1；订单完工后学徒正确拿到
  `mentor_points_earned` 算出的点数（师傅细心 18 → 2 点，师傅细心 24 → 3 点，与公式吻合）；
  分配按钮点击后待分配点数 -1、目标属性 +1，整数、不重复扣。
  下一步：车型按属性加权耗时（另一块 08-11 留下的老项）；经验值机制何去何从仍未讨论；带教
  比例/系数（0.2、1、10）都是占位值，之后跟其他数值一起平衡。

  落地后拉起游戏给用户实机体验，用户当场反馈："带教修车"这个单独按钮可以取消——如果学徒
  绑定了师傅，师傅正常接单干活时就该自动算带教（同时因为有徒弟在旁效率更高），不需要每次
  单独选人触发一次性动作。这是把"带教"从一次性动作改成"师傅↔学徒长期绑定关系"的设计调整：
  工人新增 `mentor_apprentice_id` 字段（-1 表示未绑定），`GameState.gd` 加了
  `bind_mentor`/`unbind_mentor`（绑新学徒时自动把这个学徒从原师傅那解绑，一个学徒同时只能
  被一个师傅带）/`bound_apprentice_ids`/`mentor_of`。`Main.gd` 删掉了整个"带教修车"按钮和
  配对下拉，改成 `_on_repair_button_pressed`（也就是玩家平时接单的按钮）在挑到空闲工人后，
  顺手检查这个工人有没有绑定学徒、学徒当前是否空闲——空闲就自动按带教流程走（耗时打折、
  完工发属性点），学徒在实习/训练/考试等忙碌状态时师傅照常独自接单，不强求。原有
  `_on_job_complete` 完全没改，因为它一开始就是按 job 字典里的 `apprentice_id` 字段区分是否
  带教单，不管这个字段是"一次性按钮点出来的"还是"绑定关系自动带出来的"都一样处理。
  工人列表原来只是纯文本 Label，这次改成 `HBoxContainer` 加了"选学徒下拉+带徒弟"按钮（未绑定
  时）或"解除带教"按钮（已绑定时），学徒列表里也加了"带教师傅 #X"的双向展示。
  用 godot-mcp 真实点击复测：给工人绑学徒成功、绑定关系正确显示在双方行里；点普通的"接单
  修车"（没有碰任何带教专属按钮）后自动触发带教，学徒拿到按师傅细心值算出的属性点（细心 18
  → 2 点，和公式吻合）；点"解除带教"后绑定正确清空、恢复显示配对下拉。

  改完拉起游戏让用户自己上手玩，反馈了两个待改进点，留到下次处理，这次先只记录不深挖：
  - 点"接单修车"时能感觉到一下卡顿，不确定是不是刻意设计（比如某个占位延迟），需要下次
    排查具体原因。
  - 多个工人都空闲、待处理订单数 > 1 时，体感上还是一次只能接一单。`station_count()` 在
    `facility_level=0` 时恒为 1（`BASE_STATION_COUNT + STATION_PER_FACILITY_LEVEL * facility_level`
    = 1 + 1×0），这是 08-06 就定好的设计——工位数由设施等级决定、和员工人数解耦——所以现象
    本身可能是"没升级设施"导致的预期行为，而不是并发逻辑本身的 bug；但要不要在 UI 上把这个
    因果关系说得更明显（比如工位数不够时给出更直接的提示），或者重新考虑初始工位数是不是
    太小，都需要下次跟用户一起确认到底是认知问题还是设计需要调整。
- 2026-08-13：排查并修复"点接单修车卡顿"（上次留的第一块）。根因比最初猜测的"某个占位延迟"
  深得多，分两层：
  1. `Main.gd` 里每次操作会经由 `game_state` 的信号连锁触发 2~6 次完整列表重建
     （`set_employee_busy`/`add_money`/`add_reputation` 等各自 `emit()`，信号处理函数
     `_on_int_state_changed`/`_on_employees_changed` 又都调用 `_update_all()`，外加调用方自己
     再显式调一次）。改成信号只打 `_ui_dirty` 脏标记、`_process()` 里每帧最多重建一次，
     把重复次数压到 1。
  2. 就算压到 1 次，单次重建仍要 300~450ms——用 godot-mcp 逐层实测定位到：项目没有配置任何
     字体资源，`project.godot` 里没有 theme/font 设置，每个带中文的 `Label`/`Button` 排版耗时
     约 7ms，跟具体用哪个字体、字形是否已经渲染过都无关（换字体、重复渲染同样的字都一样慢），
     纯英文文字只要几十微秒——差两个数量级。十来个员工的列表一次重建要建三四十个中文文本
     节点，几百毫秒就是这么堆出来的。试过给 `project.godot` 加
     `internationalization/rendering/text_driver="Fallback"` 切换到 Godot 内置的轻量文字引擎，
     但 `TextServerManager` 查出来这个 Godot 4.7.1 官方构建只编译了 `Dummy`（不渲染文字）和
     `ICU / HarfBuzz / Graphite` 两个后端，没有真正的轻量 Fallback 可切，这条路堵死，已把这条
     设置改回去。
  真正生效的修复：把"每次状态变化就整个列表 `queue_free()` + 重建"改成常驻行控件、按下标
  复用——工人/学徒目前只增不减（没有解雇机制），下标天然稳定，不需要按 id 做通用 diff。
  `_update_worker_list`/`_update_apprentice_list` 现在只在算出来的文字**真的变了**时才写
  `.text`（这才是触发中文排版开销的动作），按钮的 disabled/visible 之类纯布尔状态随便更新
  没有排版成本；训练课程按钮、属性分配按钮文字固定不变，只建一次，之后只切换
  disabled/visible。工人行的带教操作区（解除带教 vs 学徒下拉+带徒弟）结构会随绑定状态变化，
  单独判断"mentor_id 或可选学徒列表变了"才重建那一小块，其余部分不受影响。
  用 godot-mcp 实测（6 工人 + 4 学徒 + 2 组带教绑定）：修复前单次"接单修车"点击那一帧
  frame_time 438ms；只做第 1 层修复（合并重建次数）后单次重建仍要 438ms（因为本来就只有
  1 次重建，压根没有多余重建可合并，说明第 2 层才是大头）；加上第 2 层修复后同样的点击
  降到 36ms，FPS 全程稳定 230+，状态没变化的空闲帧开销降到 0.2ms 左右。真实点击复测了完整
  流程（起单→带教自动触发→完工→属性点发放），UI 显示内容和 `game_state` 完全一致，没有
  引入新 bug。
  下一步：另一块"体感上一次只能接一单"还没动，需要先跟用户确认这是不是"没升级设施"导致的
  预期行为、要不要在 UI 上把工位数因果关系说得更明显。

  同一晚上用户实机上手测试后反馈："有多个订单的时候，点击接单，还是会卡顿一下，很容易就
  错过另一个订单，而且无法分配汽车给特定的工位"。用 godot-mcp 复现排查，结论跟用户最初的
  直觉不一样：**不是卡顿导致抢单失败**。实测 `_update_order_label()`（订单倒计时那个 Label）
  只要 26 微秒——它是场景里本来就存在的常驻节点，只是重设 `.text`，不新建节点，所以不受
  中文排版开销影响；小规模场景（2 工人）下真实点击 frame_time 只有 14ms，也没有明显卡顿。
  真正原因是：点第一次"接单修车"后 `repair_button.disabled` 立刻变 `true`——因为
  `station_count()=1`（设施等级 0）、`active_jobs.size() >= station_count()`，按钮被禁用，
  第二次点击压根没反应。也就是说"卡顿感"和"抢不到第二单"是被一起体验到的两件不相关的事，
  后者其实就是上次会话就留的"一次只能接一单"那个问题——现在实测坐实了确实是设计如此
  （工位数由设施等级决定），不是并发 bug。
  这也解释了"无法分配汽车给特定工位"：现在压根没有"工位"这个可见实体，待处理订单也只是
  一段文字罗列，唯一的"接单修车"按钮永远无脑抓队列第一单配第一个空闲工人，玩家没有任何
  选择权。这跟"员工要是具体个体、不是数字"（见 [[project_skeleton_before_numbers]]）是
  同一类设计缺口，只是这次轮到订单和工位。
  跟用户对齐方向：**工位做成可见实体，工人绑定到具体工位**（复用带教那次"标准关系优于
  一次性动作"的模式——绑一次、持续生效，不是每次手动选）；**每个待处理订单有自己的按钮**，
  点了就是把这单分配给某个工位，工位上绑了工人就自动开工。用户选择在下次会话再动手实现，
  这次先只记录方向、不写代码。
  下一步（下次会话）：把"工位"从纯数字变成可选择的实体列表（类似工人/学徒列表的做法）；
  工人-工位绑定关系（`bind_worker_station`/`unbind_worker_station`，参考
  `bind_mentor`/`unbind_mentor` 的实现模式）；订单列表从纯文本 Label 改成逐条可点击的按钮，
  点击后把该订单分配给指定工位。旧的"一个大接单按钮无脑抓队首"逻辑（`_on_repair_button_pressed`
  自动挑 `idle_workers()[0]` + `pending_orders[0]`）预计会被整个替换掉。
- 2026-08-15：落地上次对齐的方向——工位实体化 + 按订单分配。`GameState.gd` 新增 `stations`
  数组（`{id, worker_id: -1 表示未绑定}`），随设施升级同步增长（`_sync_station_count`，只增
  不减，不需要删除逻辑）；新增 `bind_worker_station`/`unbind_worker_station`（一个工人同时只能
  绑一个工位，绑新工位前自动从原工位解绑，实现模式照搬 `bind_mentor`/`unbind_mentor`）、
  `idle_stations`（工位有工人绑定且该工人当前空闲才算能接单）、`station_of_worker`、
  `bound_worker_ids` 等查询函数。
  `Main.gd` 删掉了 `RepairButton`/`OrderLabel`/`_on_repair_button_pressed`/`_can_start_job`，
  换成：工位列表（`StationListContainer`，工人只增不减、按下标复用的常驻行模式，同 08-13
  的工人/学徒列表）——每行显示状态+倒计时+绑定工人，未绑定时给一个工人下拉+绑定按钮，已绑定
  给解绑按钮，跟工人行的带教绑定 UI 是同一套模式；订单列表（`OrderListContainer`，固定
  `QUEUE_CAPACITY` 个常驻行、按下标复用，不用的行隐藏而不是删节点）——每行显示订单信息，
  给一个"空闲工位"下拉 + 分配按钮，没有空闲工位时显示"无可用工位"提示而不是隐藏按钮。
  任务开始时 `job` 字典新增 `station_id` 字段（模式同 `apprentice_id`）：工位状态显示走
  `_find_job_by_station` 查这个固定字段，不依赖当时的绑定关系，所以工作中途解绑工位不会
  弄丢这个任务的倒计时显示、也不影响工人本身的忙碌状态和完工结算——两者是独立的两件事。
  倒计时文字沿用 08-13 定下的模式：常驻 Label 每帧直接重设 `.text`（不新建节点，经测开销
  可忽略），下拉/按钮这类结构性内容才挂 `_ui_dirty` 脏标记按需重建。
  用 godot-mcp 真实点击复测：工位绑定/解绑（含"先展开下拉选中，再点绑定"和"直接点绑定"
  两种操作顺序都验证过）；订单指定分配到特定工位（不是自动抓第一个）；用 `game_time_scale`
  把游戏时钟调到 0.05 倍验证了两个工位真正同时独立接单在跑（之前"一次只能接一单"的体感问题
  在这次改动后已解决，工位数只要 ≥2 就能并发）；工位数随设施升级从 1 涨到 2 时新工位行正确
  出现在列表里；用 `game_call_method` 直接调 `unbind_worker_station` 验证了"工作中解绑"这个
  边界情况——工位显示仍正确带着倒计时、工人忙碌状态不受影响、任务正常结算，没有出现状态
  不一致或崩溃。
  下一步：车型按属性加权耗时、经验值机制去留、带教/工位相关数值整体平衡，这几块还是老账，
  仍未动。
- 2026-08-16：落地"车型按属性加权耗时"（上次留的老账第一项）。先跟用户对齐了两个设计点：
  ①车型和工人硬技能怎么对应——选了"单一主技能"（家用轿车→机械、SUV→钣喷、跑车→电气），
  呼应最初核心循环草案里"特定车型需要特定技能"的设想，比三项加权组合更简单、之后好平衡；
  ②技能值怎么换算成耗时倍率——选了"以基准值为中心线性双向浮动"：基准 20（贴近
  `WORKER_ATTR_MIN`/`MAX`=15~25 的中点），每高/低于基准 1 点耗时 ±3%，钳制在 [0.7x, 1.3x]。
  实现：`OrderType.gd` 新增 `primary_attribute` 字段，三个车型资源各自指定对应技能；
  `GameState.gd` 新增 `repair_time_multiplier(attribute_value)` 公式函数和相关常量
  （`REPAIR_TIME_BASELINE_ATTR`=20、`REPAIR_TIME_PERCENT_PER_POINT`=0.03、钳制区间
  [0.7, 1.3]）；`Main.gd` 的 `_start_job` 按工人在该车型主技能上的属性值算出倍率乘进
  `timer.wait_time`，跟带教折扣（`MENTOR_TIME_REDUCTION_RATIO`）是连乘关系、互不冲突。
  刻意收窄范围：这次只影响工位上工人的正式接单耗时，学徒"实习"（`_on_practice_button_pressed`）
  没有接入——学徒起始属性全是 5（远低于基准 20），套同一个基准会导致学徒实习恒定最慢，
  性质上更像是需要单独设计的另一件事，留到以后专门讨论学徒独立接单表现时再处理。
  用 godot-mcp 实测：`game_eval` 验证公式边界值（属性 26→0.82x、20→1.0x、10→1.3x 钳制生效）；
  三种车型分别用主技能验证了实际 `timer.wait_time` 跟手算耗时精确吻合（家用轿车机械 26→0.82s、
  SUV 钣喷 12→2.48s、跑车电气 30→2.45s 钳制生效）；真实点击"分配"按钮走完整 UI 流程，SUV 订单
  正确按打折后的耗时倒计时。过程中又踩了两次已知的 `game_eval` 老坑：一次是多行代码里混用
  tab/space 缩进导致调试器卡死，一次是引用了已经自然完成、被 `_on_job_complete` 自动清理掉的
  `active_jobs[0]`（数组已空，越界报错）——都不是这次改动引入的新问题，用 `stop_project`
  重启后照常继续测试。
  下一步：经验值机制去留、带教/工位相关数值整体平衡仍是老账；新冒出的"学徒独立接单是否也要
  接属性加权"留到专门讨论学徒表现时处理。
- 2026-08-17：处理"经验值机制去留"（上次留的老账第一项）。给用户列了四个方向（转化为属性点/
  直接移除/保留但重新赋新用途/先不决定），选定"经验转化为属性点"——学徒"实习"不再产出独立的
  经验值/等级门槛，改成跟带教一样直接产出待分配属性点，玩家自己决定分给哪项，三条成长路径
  （自学训练/带教/实习）在"产出可分配属性点"这件事上终于统一了。
  实现：`GameState.gd` 新增 `practice_points_earned(reputation_gain)`（直接沿用车型的
  `reputation_gain` 当难度系数，家用轿车 1 / SUV 2 / 跑车 3，量级贴近带教点数，暂不加额外
  系数），删掉了已经没有消费者的 `apprentice_xp_required_for_level()`/`add_apprentice_xp()`
  和 `APPRENTICE_XP_BASE`/`APPRENTICE_XP_LEVEL_STEP` 常量，员工字典（工人/学徒）都去掉了
  `xp` 字段。`Main.gd` 的 `_on_practice_complete` 改成调用 `add_attribute_points`
  （复用带教那套），学徒状态文字里的"经验 X/Y"展示段也删掉。`level` 字段本身没动——它早在
  08-11 就已经改成靠考试晋升、不再消费经验值，这次只是把"实习"这个仍在给已死的 xp
  充数的产出口，改接到真正有效的属性点系统上。
  用 godot-mcp 实测：雇工人+学徒后员工字典确认没有 `xp` 字段；真实点击"实习"完成后
  `pending_points` 从 0 变 1（家用轿车 reputation_gain=1，与公式吻合）；UI 正确从"经验 X/Y"
  变成"待分配点数 1"，并弹出跟带教一样的 +属性分配按钮；真实点击"+机械"后属性 5→6、
  待分配点数归零，整条链路（实习产出点数→玩家手动分配→属性生效）验证通过。

  改完上面这版，用户追问"这个实习要不要直接去掉呢？实习到底算是什么"——顺势把"实习"和
  "训练"/"带教"摆在一起对比：花钱还是赚钱、要不要师傅、要不要占工位/吃真实订单、属性成长
  方式。发现这次改完之后，实习和带教在"产出待分配属性点"这件事上已经完全趋同，实习唯一还
  留着的独特定位是"不需要师傅/工位/真实订单，学徒能自己闷头练"——一个纯粹的兜底手段。列出
  四个方向（保留当兜底/去掉并入训练带教/重新往"能接真实客户单"改造/先不决定）问用户，
  选择"去掉，并入训练/带教"——直接删除，不留兜底。
  删除范围：`Main.gd` 的 `_on_practice_button_pressed`/`_on_practice_complete`、
  `active_practices` 数组、学徒行的"实习"按钮及其状态展示分支、`APPRENTICE_PAYOUT_RATE`
  常量；`GameState.gd` 的 `practice_points_earned()`（上一版才加，随即被这版删掉）。
  `_get_available_orders()` 保留，因为订单队列生成逻辑也在用。
  用 godot-mcp 实测：雇工人+学徒后学徒行只剩"考试"+训练课程按钮，"实习"按钮确认消失；
  绑定带教关系、绑定工位、给待处理订单点"分配"这条主链路走了一遍，确认没被误伤——分配后
  自动带教触发、学徒 `pending_points` 按师傅细心值正确到账（细心 23 → 3 点）、金钱正确入账，
  训练/考试按钮也都还在正常响应。
  下一步：带教/工位相关数值整体平衡、学徒独立接单是否接属性加权耗时（这条现在语境变了——
  已经没有"学徒独立接单"这回事，可能需要重新表述或直接划掉），两块老账继续留着。
- 2026-08-18：划掉"学徒独立接单是否接属性加权耗时"这条老账——08-17 去掉实习功能后，
  学徒已经没有"独立完成真实订单赚钱"这回事了（只剩带教/训练/考试），这条问题本身的前提
  已经不存在，没有代码改动，直接从待办清单里移除。
  下一步：数值整体平衡（带教系数、工位/设施数值、训练课程数值、考试门槛等占位值），
  从哪块具体数值切入待跟用户对齐。

  当晚开始逐项过数值平衡清单，从①带教系数入手。讨论中发现带教固定 20% 耗时折扣
  （`MENTOR_TIME_REDUCTION_RATIO`）跟 08-16 加的"车型按主技能加权耗时"是两条平行机制，
  都在影响耗时，有点重复。用户提议：去掉这个固定折扣，耗时完全交给工人自己的技能属性值；
  "特定技能减少维修时间"这类效果以后走专门设计的技能系统（区别于现有五维属性），先记方向、
  不在这次实现。据此删掉了 `MENTOR_TIME_REDUCTION_RATIO` 常量和 `_start_job` 里应用它的那行，
  带教的属性点产出（`mentor_points_earned`，师傅细心值决定点数）本身没动。
  用 godot-mcp 实测：绑定带教后接单，`timer.wait_time` 精确等于
  `order.repair_time × repair_time_multiplier(工人技能值)`（机械 30 → 0.7x），确认没有再叠加
  额外折扣；`job` 字典里 `apprentice_id` 正确记录，完工后属性点发放这条链路未受影响。
  未来技能系统（角色专属技能减耗时等）记为新的设计方向，具体设计留到以后。
  下一步：数值平衡清单剩余项——工位/设施数值、训练课程数值、考试门槛、招聘/工资、
  车型接单相关数值，继续逐项过。

  紧接着过②工位/设施数值。列出当前设施 0~3 级的费用/工位数/收入倍率表格给用户看
  （150 起步、每级 +100；工位 1+1/级；收入倍率 1+0.15×级），用户反馈工位应该封顶，
  提出"最多 5 个"。确认封顶方式：工位封顶后设施本身要不要也跟着封顶——选了"设施也一起
  封顶"（而非"只封工位、设施可以无限升级换纯收入加成"），5 级设施（工位 5、收入倍率
  1.60x）就是终局形态，呼应"设施线要有点满的终点感"。
  实现：`GameState.gd` 新增 `MAX_STATION_COUNT=5`、`max_facility_level()`
  （`(MAX_STATION_COUNT-BASE_STATION_COUNT)/STATION_PER_FACILITY_LEVEL`=4）、
  `is_facility_maxed()`；`upgrade_facility()` 到顶后直接返回 false，不再扣费。
  `Main.gd` 的 UI 刷新逻辑加了满级分支：按钮文字变"已满级"并禁用，不再显示下一级费用。
  用 godot-mcp 实测：连续调用 6 次 `upgrade_facility()`，前 4 次成功（level 0→4，工位
  1→5，倍率 1.0→1.60x），第 5/6 次正确失败、状态不再变化；真实点击升级按钮复测，UI
  正确显示"设施等级: 4（工位数 5）"+ 灰色"已满级"按钮，5 个工位行也都正确渲染出来。
  下一步：数值平衡清单剩余项——训练课程数值、考试门槛、招聘/工资、车型接单相关数值。
- 2026-08-19：继续过数值平衡清单，先看③训练课程数值——五门课程参数完全对称（花费20/耗时8秒/
  基础成长8点，衰减公式贴合属性软上限150），用户确认暂时不用改。

  接着看④考试门槛，列出门槛公式 `100+40×level` 和通过率公式给用户看，顺带发现"通过考试"
  除了让工资变贵、门槛变高之外没有任何正向解锁（不转正、不加成、不影响接单），用户也觉得
  单薄。讨论后重新定位考试为"资质认证"：学徒第一次通过考试即转正为"普工"，能像正式工人一样
  独立绑工位接单，但工资是普通工人的 80%（店里已经为这个人花过培养成本，算内部培养折扣价）；
  转正后自动解绑师傅，但还能继续上训练课/考更高职级（工程师等更高职级设计这次先不做）。
  另外加了学徒等级封顶 `MAX_APPRENTICE_LEVEL=10`，到顶后不能再考试（之前门槛是无限往上涨的）。

  实现：`GameState.gd` 学徒字典加 `qualified` 字段（默认 false）；`take_exam()` 第一次通过时
  置 `qualified=true` 并调用新增的 `_unbind_apprentice_from_mentor()` 自动解绑师傅；
  新增 `qualified_apprentice_salary()`（`WORKER_SALARY_PER_HEAD × QUALIFIED_SALARY_RATIO`=16）
  接入月薪结算；`bind_worker_station()` 放开成"kind=worker 或 qualified 的学徒"都能绑；
  `bind_mentor()` 拒绝已转正学徒重新拜师；新增 `station_eligible_employees()`（workers()+已转正
  学徒）给工位分配面板用，不影响 `workers()`/`apprentices()` 原有语义（带教师傅资格还是只认
  正式工人）。`Main.gd` 工位分配面板改用 `station_eligible_employees()`；师傅拜徒弟下拉框
  过滤掉已转正学徒；学徒行文案加"[已转正]"标签、月薪按转正与否切换、补上工位号显示、
  忙碌状态分支补上"独立接单工作中"（原来只有训练中/带教中两种，转正学徒自己接单会漏判）。
  用 godot-mcp 实测：绑师傅→属性拉到门槛→考试通过，确认 `qualified` 变 true、师傅
  `mentor_apprentice_id` 自动归 -1、月薪变 16、能绑工位（`station_eligible_employees()`
  正确包含转正学徒）、重新拜师失败（`bind_mentor` 返回 false）；把 level 设到封顶 10，
  确认 `can_take_exam` 返回 false；截图复核 UI 文案（"[已转正] 学徒 #2：Lv10，月薪 16...
  工位 #1..."）全部正确，考试按钮正确置灰。

  下一步：数值平衡清单剩余项——招聘/工资、车型接单相关数值，继续逐项过。
  另外新增一条待办：员工目前只有编号（工人#1/学徒#2），没有名字，之后要做员工命名系统，
  让每个人有真正的身份（呼应"员工是个体不是数字"这条一直在遵循的方向）。
- 2026-08-20：继续过数值平衡清单，看⑤招聘/工资数值。列出现状给用户看：雇佣成本随人数
  递增（工人 100/150/200…，学徒 30/40/50…），工资相对订单收入非常便宜（一个工人几秒钟
  接一单就能覆盖一个月工资），瓶颈更多在雇佣成本和订单生成频率上。

  用户反馈递增费用不真实——不应该因为多雇一个人就把工资行情抬高，改为"价格大致固定，
  但工人/学徒总数受口碑声望限制"。讨论后定下设计：工人和学徒**共用一个编制名额池**（而
  不是各自独立上限）；学徒转正后继续占用编制坑位，不释放（相当于身份从学徒变工人，但
  没有腾出新学徒名额）。给出提案：固定价格（工人 100、学徒 30，沿用原起步价当基础价），
  编制上限 `2 + floor(声望 / 15)`，封顶 5——起步 2 个坑保证开局能雇 1 工人 + 1 学徒，
  45 声望时封顶，正好和 `MAX_STATION_COUNT=5` 对齐，避免编制超过工位数、招了人却没工位
  可用。用户确认这版提案可以。

  实现：`GameState.gd` 删除 `BASE_HIRE_COST`/`HIRE_COST_STEP`/`BASE_APPRENTICE_HIRE_COST`/
  `APPRENTICE_HIRE_COST_STEP` 递增费用常量，改为固定的 `WORKER_HIRE_COST=100`、
  `APPRENTICE_HIRE_COST=30`；新增 `HEADCOUNT_BASE=2`、`REPUTATION_PER_HEADCOUNT_SLOT=15`、
  `MAX_HEADCOUNT=5` 及 `max_headcount()`/`total_headcount()`；`hire_worker()`/
  `hire_apprentice()`/`can_hire_apprentice()` 都改成检查编制上限（`total_headcount() <
  max_headcount()`）而不是只看钱够不够；新增 `can_hire_worker()` 给 UI 用。`Main.gd`
  雇佣工人按钮的禁用判断改用 `can_hire_worker()`；顶部状态栏加"编制: X/Y"显示。
  用 godot-mcp 实测：初始声望 0 编制上限 2，雇满后 `can_hire_worker`/`can_hire_apprentice`
  正确变 false，超编制强行雇佣正确失败；声望提到 15 上限变 3，立刻能再雇一个；声望提到
  45/200 上限都封顶在 5；雇佣价格多次调用保持固定不变（100/30）。截图复核 UI 正确显示
  "编制: 3/5"。

  下一步：数值平衡清单最后一项——车型接单相关数值。
